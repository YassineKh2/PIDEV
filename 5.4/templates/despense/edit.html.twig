{% extends 'baseBack.html.twig' %}

{% block title %}Edit despense{% endblock %}

{% block body %}
<div class="editform">
    <h1>Editer despense</h1>

    {{ include('despense/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_despense_index') }}">Precedent</a>

    {{ include('despense/_delete_form.html.twig') }}
</div>
    <style>
        .editform{
            width: 1150px;
            position: relative;
            margin-top: 200px;
            left: 300px;
        }
    </style>
{% endblock %}
