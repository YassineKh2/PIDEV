{% extends 'base.html.twig' %}

{% block title %}Therapist{% endblock %}

{% block body %}
    <h1>Therapist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ therapist.id }}</td>
            </tr>
            <tr>
                <th>NumTelTherapist</th>
                <td>{{ therapist.NumTelTherapist }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_therapist_index') }}">back to list</a>

    <a href="{{ path('app_therapist_edit', {'id': therapist.id}) }}">edit</a>

    {{ include('therapist/_delete_form.html.twig') }}
{% endblock %}
